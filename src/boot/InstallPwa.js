// PWA Install disabled for Table Order app
// Customers scan different table QR codes each visit, so installing provides no benefit

export default () => {
  // No-op: PWA install prompt disabled for table ordering
};
